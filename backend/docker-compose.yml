version: "3"
services:
  pg:
    image: postgres:11.4-alpine
    ports:
      - 5400:5400
    restart: on-failure
  
  server:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./:/app/server
    ports:
      - 8000:8000
    depends_on:
      - pg
    restart: always